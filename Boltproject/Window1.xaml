<Window x:Class="BoltWPF.UjTermekWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Új termék" Height="300" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Label Content="Azonosító:"/>
        <TextBox x:Name="AzonositoTextBox" Grid.Row="1" Margin="0,5"/>

        <Label Content="Név:" Grid.Row="2"/>
        <TextBox x:Name="NevTextBox" Grid.Row="3" Margin="0,5"/>

        <Label Content="Darabszám:" Grid.Row="4"/>
        <TextBox x:Name="DarabszamTextBox" Grid.Row="5" Margin="0,5"/>

        <StackPanel Orientation="Horizontal" Grid.Row="6" HorizontalAlignment="Right">
            <Button Content="Mégse" Click="Mégse_Click" Margin="5"/>
            <Button Content="Mentés" Click="Mentés_Click" Margin="5"/>
        </StackPanel>
    </Grid>
</Window>